import { BlogSection as GlobalBlogSection } from "@/components/sections/BlogSection";

/** Home-page variant — shows the three latest posts across all industries. */
export function BlogSection() {
  return <GlobalBlogSection />;
}
