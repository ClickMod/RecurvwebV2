"use client";

import { CallSection } from "@/components/sections/CallSection";

/** Contact-page variant — uses all Recurv defaults, no props needed. */
export function ContactCallSection() {
  return <CallSection />;
}
